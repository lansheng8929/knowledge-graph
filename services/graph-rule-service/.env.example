# 存储后端：memory（开发/测试，默认） | postgres（生产）
RULE_STORE=memory

# PostgreSQL（RULE_STORE=postgres 时使用）
PG_DSN=postgresql://rule:rule@localhost:5432/rules

# HTTP
HOST=0.0.0.0
PORT=8002
SERVICE_VERSION=0.1.0

# CORS 开发白名单
CORS_ALLOW_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
