[project]
name = "audit-sink"
version = "0.1.0"
description = "Audit sink (log | file | postgres) for PEP decisions"
readme = "README.md"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["audit_sink"]
