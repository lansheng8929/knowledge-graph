[project]
name = "service-common"
version = "0.1.0"
description = "Knowledge graph platform shared service code (JWT, subject parsing)"
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["service_common"]
