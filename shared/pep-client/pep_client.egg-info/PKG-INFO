Metadata-Version: 2.4
Name: pep-client
Version: 0.1.0
Summary: Python ABAC PEP client (local reference, not published)
Requires-Python: >=3.11
Description-Content-Type: text/markdown

# shared/pep-client — Python ABAC PEP 客户端（本地引用，不发布）

> 对应架构 §3.4.1/3.4.3 / 计划 T4.3

## 职责

封装「组装属性 → 调 OPA(PDP) → 解析决策 → 写审计」：
- `PepClient(opa_url)` 调 `POST {opa_url}/v1/data/kg/allow`
- **fail-closed**：OPA 不可达 → deny（审计 `reason=opa_unavailable`）
- 每次决策写独立 `audit` logger（`subject/resource/action/decision/reason`）

## 接入服务

```bash
# 各模块后端安装（本地引用）
pip install -e ../shared/pep-client
```

```python
from pep_client import PepClient

pep = PepClient(opa_url="http://localhost:8181")
allow = pep.check(
    subject={"tenantId": "t1", "clearance": 2, "uid": "u1"},
    resource={"tenantId": "t1", "classification": 1, "visibility": "internal"},
    action="expand",
)
```

## 分层（PEP 执行点）

- **L1 网关**：Nginx `/_authz` + `auth_request`（T4.4）
- **L2 服务**：各服务用 `pep.check` 做规则级授权（T4.5）
- **L3 数据级**：查询改写 + 投影过滤（T4.6，服务内实现）
- **L4 字段级**：脱敏（T4.7）

## 状态

- [x] `PepClient`（check / _query / _audit，fail-closed）
- [ ] 各服务接入（L2）
- [ ] 策略 bundle 下发
